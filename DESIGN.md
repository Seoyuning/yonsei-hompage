# DESIGN.md — 연세대학교 기계공학부 홈페이지

편집 지면(editorial) 톤의 대학 학부 홈페이지. 공모전 제출물.
칠과 그림자를 쓰지 않고 **가로줄과 여백으로 구획을 나눈다.**

## 모드

- 메인(`H-academic.html`) — **Persuade**. 첫 화면에서 학부가 무엇을 하는 곳인지 보여 준다.
- 나머지 일곱 페이지 — **Read**. 사실을 찾으러 온 사람이 빨리 찾게 한다.

## 색

| 이름 | 값 | 쓰임 |
|---|---|---|
| `--navy` | `#1a3d75` | 유일한 강조색. 제목·링크·활성 표시 |
| `--navy-d` | `#12294f` | 히어로·형제바·짙은 밴드 |
| `--ink` | `#0f1b30` | 본문 글자 |
| `--sub` | `#41506b` | 부제·설명 |
| `--muted` | `#5e6b82` | 보조 글자 (5.38:1) |
| `--dim` | `#6b7688` | 라벨 (4.59:1) |
| `--label` | `#6a7486` | 표 라벨 (4.71:1) |
| `--line` | `#e2ddd2` | 헤어라인 |
| `--accent` | `#A56F63` | 장식(점·테두리)에만. 글자에는 쓰지 않는다 |
| `--paper` | `#ffffff` | 바탕 |

흰 바탕 위 모든 글자색은 **4.5:1 이상**을 지킨다.

## 글자

- 한글·영문 모두 `Apple SD Gothic Neo` / `Pretendard`.
- **UI 글자 최소 11.5px(`.72rem`)**. 그 아래로 내려가지 않는다.
- 대제목 `clamp(1.7rem, 1.2rem + 2.4vw, 2.9rem)` · 800 · `letter-spacing:-.02em`
- 히어로 제목 `clamp(2.1rem, 1.25rem + 3.4vw, 3.9rem)` · 800
- 본문 `clamp(.88rem, .84rem + .16vw, .98rem)` · 500 · `line-height:1.75`

## 폭

**모든 컨테이너는 `max-width:77rem`(기본 뿌리 16px에서 1232px) + `padding:0 clamp(1.2rem,.6rem + 2vw,2.6rem)`.**
히어로·형제바·필터·본문이 같은 왼쪽 선에 선다. 예외는 상단 바 하나 —
`max-width:none`으로 화면 끝까지 쓴다(학부 이름이 왼쪽 끝, 메뉴가 오른쪽 끝).

**PC 와이드(≥1680px)는 뿌리 글자로 비례 확대한다** — fonts.css의
`html{font-size:clamp(16px, 10.28px + .34vw, 19.5px)}` 하나로 글자·여백·컨테이너(77rem)가
함께 큰다. 1920에서 약 +5%, 2560(QHD)에서 +19%. 컨테이너 절반을 셈할 때는
`50vw - 38.5rem`처럼 rem으로 — px(616px)로 고정하면 확대를 따라오지 못한다.

## 되풀이되는 짜임

**섹션 머리** — 이 셋의 순서를 바꾸지 않는다.
```
RESEARCH AREA          ← 작은 영문 라벨(.72rem, letter-spacing .22em, 대문자)
────────────────────   ← 먹색 1px 실선
연구 분야               ← 대제목
```

**페이지 히어로** — 일곱 페이지가 같다.
```
홈 › 연구              ← breadcrumb
RESEARCH               ← 영문 라벨
연구                    ← 대제목 (좌측 정렬, 아래쪽 붙임)
```

**상단 바** — 메인 히어로 위에서는 투명(글자 흰색), 히어로를 지나기 120px 전에 흰 바탕.
하위 페이지는 늘 흰 바.

**형제바(`.ysub`)** — 히어로와 같은 네이비로 이어 붙인다. 흰 띠로 끊지 않는다.

**표시(accent)** — 활성 항목은 **왼쪽 또는 아래에 2~3px 네이비 기둥**.
칠·둥근 배지·알약 모양을 쓰지 않는다.

## 일부러 어기는 것

아래는 이 지면의 정체성이다. 검사 도구가 짚어도 고치지 않는다.

- **`cramped-padding`** — 구획을 `border-top`/`border-bottom` 헤어라인으로만 나눈다.
  줄 위아래에 여백을 넣으면 편집 지면 톤이 사라지고 카드 UI가 된다.
- **`hero-eyebrow-chip`** — 대제목 위 작은 영문 라벨은 **우리가 정한 통일 형식**이다.
  여덟 페이지 전부와 모든 섹션 머리가 이 형태다.
- **`all-caps-body`** · **`wide-tracking`** — 위 영문 라벨의 `text-transform:uppercase`와
  `letter-spacing:.22em`. 짧은 라벨에만 쓰고 본문에는 쓰지 않는다.
- **`marquee`** — 메인의 인스타그램 띠. 실제로 흐르는 소식이라 흐르게 둔다.
- **`flat-type-hierarchy`** — 크기 대비 대신 **굵기와 색**으로 층을 만든다(800/700/500).
- **`side-tab`** — 위의 「왼쪽 네이비 기둥」이 바로 이것이다.
- **`em-dash-overuse`** — 한국어 설명문에서 줄표(—)를 삽입구로 쓴다.
- **`dark-glow`** — 그림자 색을 검정이 아니라 먹색(`--ink` `#0f1b30`)에서 가져와 쓴다.
  네이비 지면 위에서 검정 그림자는 회색 때처럼 뜬다. 모두 아래로 치우친(y-offset)
  올림 그림자이고, 이 규칙이 실제로 겨냥하는 「치우침 없는 색 후광」은 한 곳도 없다.
- **`repeating-stripes-gradient`** — 연구 화면 독수리 무대 뒤의 「도면 층」 두 겹.
  기계 도면의 모눈이라 이 화면의 정체성이다. 화면 밖에서는 멈춘다.
- **`low-contrast` (사진 위 흰 글자)** — 학사 화면 배너 캡션. 흰 글자 뒤에
  `rgba(8,17,34,.82)` 까지 짙어지는 그라데이션 막이 이미 깔려 있다.
  검사 도구가 그라데이션을 못 읽어 「흰 바탕 위 흰 글자」로 잡을 뿐이다.

## 반드시 지키는 것

- 흰 바탕 글자 대비 **4.5:1** 이상
- UI 글자 **11.5px** 이상
- 움직임은 `transform`·`opacity`만. `border-radius`·`box-shadow`·`filter`를
  프레임마다 바꾸지 않는다(휴대폰에서 매 프레임 다시 칠한다)
- `prefers-reduced-motion`에서 자동 넘김·스크롤 연동을 끈다
- 모든 화면을 휴대폰(390px)에서 먼저 확인한다
