# 작업 체크포인트

> PC가 꺼지거나 세션이 끊겨도 여기서부터 이어서 작업한다.
> **작업을 시작·전환·완료할 때마다 이 파일을 갱신하고 커밋한다.**
> 프로젝트 전체 상태는 `README.md` 의 「현재 상태 & 다음 할 일」, 편집 도구 계약은
> `design-candidates/STUDIO_SPEC.md` 를 본다. 이 파일은 **지금 진행 중인 것**만 담는다.

---

## 마지막 갱신

| 항목 | 값 |
|---|---|
| 시각 | **2026-07-26 (일) 18:07 KST** (UTC 09:07) |
| 커밋 | `267e014` feat(studio): 잠긴 텍스트 칸 해소 + 공지·소식 등록 |
| 원격 | `origin/main` 과 동기 (push 완료) |
| 배포 | `yonsei-me-homepage` 프로덕션 Ready — 자산 5종 200 확인 |
| 진행 중 | **편집 UX 3/3 + 디자인 2a·2b·2c 완료** (2026-07-26 20:30). 요청 4건 모두 반영 |

### 디자인 2c 완료 — "AI 티" 정리

- **히어로**: 사진 전체에 평면 남색 62→82% 워시를 덮어 뭉개져 있었다. 처음엔 사진이 잘
  보이게 바꿨는데 **오히려 스톡 티가 더 났다**("다국적 학생들이 웃는 강의실" 전형이 드러남).
  방향을 반대로 잡아 **흑백 + 네이비 모노톤 질감**으로 눌렀다. 인물이 주인공이 아니게 된다.
  `filter` 는 글자까지 흐려지므로 사진 층(`::before`, `background:inherit`)에만 건다. 7페이지 공통.
- **카피**: G-people 히어로의 "함께 만들어가는 공동체입니다" → "전임 교수 33명이 여섯 개
  연구 분야에서 33개 연구실을 이끌고 있습니다…" 로. 영문도 함께 등록.
  (G-about 학과장 인사말·G-graduate 소개는 그 문체가 자연스러워 손대지 않았다.)
- **분야 6색**: 감으로 고르지 않고 CIELAB 색거리로 계산했다.
  기존 최소 ΔE 17.3(역학·소재 ↔ 바이오·포토닉스, 둘 다 파랑이라 구별 불가) →
  의미에 맞는 색상 영역 안에서 재최적화해 **32.6**.
  · 설계·제조 #4a7c59→#26865e · 마이크로·나노 #2d8a8a→#22a4b6
  · 로보틱스·제어 #7a5aa0→#996baa · 바이오·포토닉스 #3f6fb0→**#a84551**(파랑→자주, 최대 개선)
  · 역학·소재(네이비)·에너지·열유체(앰버)는 유지
- **접근성 회귀 수정**: 이 색들이 카드 밴드의 흰 글자 배경으로 쓰이는데
  기존에 이미 **2개가 WCAG 4.5:1 미달**이었다(에너지·열유체 3.90, 마이크로·나노 4.11).
  칩·아이콘은 선명한 색을 쓰고 **흰 글자가 올라가는 배경만** `color-mix(… 73%, #0a1633)`
  로 어둡게 섞어 전 분야 4.74 이상 확보. (사이트가 이미 color-mix 를 쓰고 있어 안전)
- 파일마다 변수 이름이 달랐다(`--c-micro` vs `--c-nanobio`, `--c-dyn`/`--c-manuf`).
  이름 통일은 위험이 커서 **값만** 맞췄다. 나중에 정리할 거리.

### 디자인 2b 완료 — 몰입 구역 커서 (`assets/cursor.js`)

- 점(즉시) + 링(관성) 두 겹. `[data-cursor-zone]` 안에서만 켜지고, `[data-cursor="…"]` 대상 위에서
  링이 커지며 그 말이 뜬다. 현재 구역 = 홈 `#areaGrid`, 연구 `#fieldGrid`.
- 안전장치: 전역 `cursor:none` 금지(JS 성공 시 붙는 `html.ycur-live` 아래에서만),
  터치·호버불가·모션최소화·폭<900 이면 생성 자체를 안 함, 스튜디오 편집 모드면 물러남,
  모든 DOM 에 `data-ys-ui`.
- 검사 7항목을 inttest 에 추가(편집 제외 표시·정렬 유지·편집 모드에서 꺼짐).

### 홈 교수진 레일 캐러셀 (2026-07-26 20:45)

- `#pGrid`(드래그 레일)를 `.prail-wrap` 으로 감싸고 좌우 원형 버튼을 얹었다.
  한 칸 = 카드 폭 + 간격(현재 253px). 끝에 닿으면 반대편으로 돈다.
- 자동 넘김 3.6초. **사용자가 한 번이라도 조작하면 영구히 멈춘다**
  (읽는 중에 화면이 움직이면 안 된다 — 자동 콘텐츠에 멈출 방법을 두라는 접근성 요구도 충족).
  호버·포커스·드래그·탭 숨김·섹션 화면 밖·편집 모드에서는 잠시 쉰다.
  `prefers-reduced-motion` 이면 자동 넘김을 아예 켜지 않는다.
- 검사: `_studio/railtest.html` (22항목). **탭이 보이는 상태에서만 의미 있다.**

### 교수진 전원 노출 + 직위 위치 변경 (2026-07-26 21:00)

- 홈 레일이 33명 중 10명만 싣고 있었다 → **전원(33명)**. 레일 길이 7234px.
- **직위(교수·부교수·조교수)를 카드 표면에서 뺐다.** 구성원 페이지의 「자세히 보기」(+)
  를 펼치면 첫 줄에 나온다.
- **보직(`role`)은 그대로 둔다** — 학과장·학부지도교수·대회 담당 3명. 직위와 성격이
  다른 정보이고, 오히려 눈에 띄어야 한다.
- 33장이 되면서 **끝→처음 되돌기가 7234px 를 부드럽게 훑고 날아가 어지러웠다.**
  되돌기만 즉시 이동으로 바꿨다(한 칸 이동은 그대로 부드럽게).
- 사진은 33명 전원 있다(`assets/faculty/*.jpg`).

### ★ 이 세션 최대의 함정 — 백그라운드 탭

MCP 브라우저 창이 뒤에 있으면 탭이 `document.hidden=true` 가 되고, 그러면 브라우저가
**requestAnimationFrame 과 부드러운 스크롤을 멈춘다.** 이것 때문에
  · 캐러셀이 "고장난 것처럼" 보였고(실제로는 정상 — 우리 코드도 hidden 이면 쉰다)
  · `scrollTo({behavior:'smooth'})` 가 전부 0 으로 되돌아왔고
  · 세션 내내 스크린샷이 백지로 나왔다.
→ 애니메이션·스크롤이 걸린 것을 검증할 때는 **헤드리스 검사 페이지**를 쓴다
  (`railtest.html` 처럼 iframe 을 화면 밖으로 밀지 말고 `opacity` 로만 숨길 것 —
   화면 밖이면 IntersectionObserver 가 꺼져 자동 넘김이 안 돈다).

### 이 세션에서 겪은 함정 (다음에 반복하지 말 것)

- **헤드리스 스크린샷이 이 사이트에서 자주 백지로 나온다.** 스크롤 리빌 애니메이션 탓으로
  보인다. 백지가 나와도 결함이 아니다 — 실제 브라우저(MCP)로 확인하거나 계산된 스타일을 읽어라.
  (이것 때문에 "헤더 아래 흰 여백"을 결함으로 잘못 지목했다가 정정했다.)
- **bash heredoc 안에서 정규식 이스케이프가 깨진다.** `\\$&` 같은 것을 넣지 말고
  Edit 툴로 직접 쓰거나 정규식을 피해라(inttest 폴백 검사에서 한 번 당했다).
- **브라우저 캐시** — nav.js 를 고친 뒤에는 Ctrl+Shift+R 로 하드 리로드해야 반영된다.

### 디자인 2a 완료 — 교수 카드 분야 아이콘

- `G-people.html`: 사진 왼쪽에 **분야 레일**(육각 6칸). 그 교수의 분야만 색으로 채운다.
  증명사진은 얼굴이 가운데라 한양대처럼 사진 위에 얹을 수 없어 전용 레일을 만들었다.
  채워진 칸이 섹션마다 아래로 내려가 "지금 어느 분야를 보는지"가 위치로 읽힌다.
  활성 칸을 누르면 그 분야만 걸러 본다(비활성 5칸은 장식 — 33장×6개를 전부 탭 정지점으로
  만들면 키보드 사용자가 괴롭다. 걸러 보기는 위쪽 칩이 정식 경로).
- `H-academic.html`: 홈 교수 카드는 작아서 6칸 대신 **분야 배지 1개**(같은 글리프·같은 색).
  홈은 교수를 무작위로 섞어 보여 주므로 배지가 더 유용하다.
- 교수당 소속 분야는 데이터상 **하나뿐**이다(교차 분야 정보 없음). 없는 걸 지어내지 않았다.

### 미번역 원인 규명 + 해소

- `en.json` 은 원래도 100% 였다. 미번역은 전부 `data.js`(한글 790개 중 사전에 23개)에서 왔고,
  추출기가 **HTML 8개 파일만** 훑었기 때문이다.
- 790개 중 ~690개는 실제 공지·뉴스·교과목·연구실 이름 = **번역하지 않는 것이 맞다**.
- 나머지 화면 라벨은 data.js 안에 이미 `ko`/`en` 짝이 있었다 → 추출기가 그 짝을 사전으로
  가져오게 했다(`ko↔en`, `rank↔titleEn`, 영문 쪽에 한글이 없을 때만).
  결과: 536 → **833항목, 미번역 0**. 공지·뉴스 제목은 영문 짝이 없어 자동으로 걸러졌다(0건 유입).
- 분야명 1건이 사전과 `clusters[].en` 이 달라 데이터 쪽으로 통일했다.

### 편집 UX 개선 완료분 (1~3번)

- 시점 비교: 코드 diff → 사람이 읽는 변경 카드 (`changes.js` 신규)
- 데이터 편집 안내: 빨간 경고 → 안내, 파일명·배열명·컨테이너 id 전부 제거
- 인스펙터 용어: 태그명·eid·CSS 브레드크럼 → 사람 말 (`Y.labels` in core.js)
- **버그 수정**: `#newsGrid`/`#ntList`/`#smList` 는 파일에 같은 클래스의 자리표시자가 있어
  정렬기가 실제 목록 첫 항목을 그 자리표시자와 짝지었다 → 고쳐도 사라지는 편집이 가능했다.
  이제 목록 안쪽 선택은 항상 목록 편집으로 가고, HTML 편집 대상은 목록 상자로 옮긴다.
- 검증: selftest 146 · inttest 123 · test-posts 28 전부 통과

### 결정된 방침 (2026-07-26 18:20, 사용자 선택)

| 항목 | 결정 |
|---|---|
| 순서 | **편집 도구(1~3) 먼저**, 그 다음 디자인(4) |
| 교수 사진 | 누끼·필터 없이 **사진은 그대로 두고 육각 분야 아이콘만 추가** |
| 동적 요소 | **절제 도입** — 몰입형 섹션에만 커스텀 커서, `prefers-reduced-motion` 존중, 터치기기 제외 |
| 히어로 스톡 사진 | **유지하되 처리 개선** — 오버레이·크롭·초점 손보기 |

## 지금 상태 한 줄

인플레이스 스튜디오의 "고칠 수 없는 칸" 문제를 없애고 공지 등록 기능을 넣어 배포까지 끝냈다.
남은 것은 **실환경 검증 3건**(공용 암호·AI 키 필요)과 **심사 제출물 마무리**다.

---

## 완료 (이번 세션, 2026-07-26)

- [x] `prototype-v3` Vercel 프로젝트 삭제 → `prototype-v3-nine.vercel.app` 404 확인
- [x] `pagedict.js` 신규 — 홈 인라인 `var I18N` 사전 오프셋 편집.
      `data-i18n` 문장을 인스펙터에서 한국어·English 두 칸으로 직접 수정
- [x] `data-count` 카운트업 숫자를 「최종 숫자」 칸에서 수정 (속성 칸 중복 제거)
- [x] `posts.js` 신규 + `datamap` 배열 삽입/삭제 — 공지·뉴스·세미나·행사 등록/삭제
- [x] `core.js` 회귀 수정 — `Y.store.get` 이 없는 키에 IDBRequest 를 반환하던 것
- [x] 문서 갱신 — `README.md`, `STUDIO_SPEC.md`(§4.4, §5.1, 합격기준 11·12), `FEATURES.md`
- [x] 검증 — selftest 146 / inttest 87 / test-posts 28, 전부 통과 · 런타임 오류 0

## 다음 후보 (미착수)

1. **실환경 검증 3건** — 공용 암호(+AI 키) 필요. 체크포인트 복원 왕복 / AI 실키 호출 / 동시편집 409
2. **심사용 데모 시나리오 + 발표 자료**
3. **구 콘솔 `admin/` 처리 결정** — 인플레이스 스튜디오와 이중 유지 여부
4. **1축 디자인 개선**
5. 편집 UX 다듬기 — 등록한 공지의 게시 전 미리보기 등

## 실환경 검증 진척 (팀원 `2026-725` 의 직접 테스트로 확인됨, 2026-07-26)

- [x] **로그인 · GH_TOKEN 권한** — `f8d8d83` 게시 성공
- [x] **게시 → 커밋** — "교수진" 제목 글자 크기 변경이 한 줄 diff 로 반영
- [x] **시점 저장** — `bd7eb36`(test1) · `fd0b8a8`(test2), `_studio/checkpoints.json` 매니페스트 동반
- [ ] **시점 복원 왕복** — 아직 미확인
- [ ] **AI 실키 호출** — API 키 필요
- [ ] **동시 편집 409**

### ⚠ 발견: 시점 복원의 커밋 조회 범위 한계

매니페스트의 `commitSha` 는 항상 `""` 다(커밋 만들기 전엔 자기 sha 를 알 수 없다 — 설계상 정상).
`versions.js shaOf()` 가 커밋 메시지의 `[ys-cp:<id>]` 표식을 **최근 60개 커밋**(`HISTORY_SCAN`)에서
찾아 sha 를 확정한다. 그보다 오래된 시점은 복원·비교가 막히고 UI 에 경고가 뜬다.

- 지금 속도(테스트 1시간에 커밋 4개)면 몇 주 만에 60개를 넘길 수 있다.
- 근본 해결안: 커밋 성공 후 `POST /git/refs` 로 `refs/ysme-cp/<id>` 를 만들어 두면
  커밋 1개 추가 없이 영구히 조회된다(히스토리 깊이와 무관).

---

## 진행 예정: 편집 UX 개선 + 사이트 디자인 보완 (2026-07-26 18:23 착수)

사용자 요구 4건:
1. 「버전 → 저장된 시점 → 현재와 비교」가 **코드 diff** 로 나온다 → 사용자 친화적 UI 비교로
2. 사용자는 디자이너가 아니라 **대학원생 조교**. `data.js` 안내 문구가 개발자 말투 · 빨간 경고라
   에러처럼 읽힌다 (실제로는 정상 라우팅 성공)
3. 전체적으로 편집이 더 쉬워야 함
4. 사이트 디자인 보완 — "AI 티" 제거. 레퍼런스: 한양대 기계공학부 학문분야 교수 UI,
   한화에어로스페이스 동적 요소

### 레퍼런스 조사 결과 (직접 열람, 2026-07-26 18:30~18:45)

**한양대 기계공학부 `research_02.html` 교수 카드**
- 분야 섹션 제목 = 큰 영문(`Thermal Engineering`) + 작은 한글(`열공학`) 병기
- 카드 좌측에 **육각형 분야 아이콘 5개 세로 스트립** — 채워짐=해당 분야 / 외곽선=미해당.
  한 장만 봐도 그 교수가 어느 분야에 걸쳐 있는지 보인다. ← 가장 훔칠 만한 아이디어
- 인물 사진은 **누끼(배경 제거)** 상반신이라 카드 배경과 이어짐
- 하단 파란 밴드에 이름·직위 + 원형 버튼 2개(홈=연구실 링크, +=상세 펼침)

**한화에어로스페이스 커스텀 커서** (`.cursor-container`)
- 자식 `.ball`(즉시 따라오는 점) + `.ring`(관성으로 지연되는 원) **2겹 구조**, `z-index:3001`
- 영역별 상태 클래스로 모양이 바뀐다: `.slide`(좌우 화살표) `.drag` `.video`(play) `.click`/`.clicked`(물결)
- 전역 `cursor:none` 은 쓰지 않는다 — 기본 커서 위에 장식을 덧씌우는 방식

**우리 G-people 현황에서 확인된 "AI 티" 구체 지점**
1. 히어로의 **무관한 스톡 사진**(다국적 학생들 강의실) + 강한 남색 오버레이 → 사진 쓴 의미 없음
2. 헤더와 배너 사이 **원인 불명의 큰 흰 여백**
3. 모든 페이지가 [영문 kicker + 한글 큰 제목 + 2줄 총론] 로 동일 = 템플릿 냄새
4. 설명문이 내용 없는 총론체 ("…함께 만들어가는 공동체입니다")
5. 필터 칩의 **6색 점**이 무채색 기조를 깬다. 초록/연두 구분 불가
6. 교수 사진의 **배경색·크롭이 제각각** (파랑/회색/검정/갈색)
7. 분야 아이콘 같은 "한눈에 보이는" 장치가 없어 카드마다 같은 글자만 반복

## 확인 필요 (사용자 결정)

- `yonsei-hompage.vercel.app` — 우리 Vercel 계정 밖(팀원 계정 추정). 저장소 루트를 서빙해
  `기획_전략/` 문서와 `admin/` 소스가 공개 URL 로 열린다. **소유자를 찾아 삭제/설정 변경 요청 필요.**
- 커밋 `f8d8d83`(팀원 게시)이 `H-academic.html` 의 "교수진" 제목에 `style="font-size:58px"` 를
  인라인으로 넣었다. 의도한 디자인 변경인지, 도구 시험 흔적인지 확인 후 유지/되돌리기 결정.

---

## 이어서 하기 — 환경 복구 절차

```bash
cd "C:/Users/thf56/Desktop/공모전 준비/연세대_사이트 공모전"
git fetch origin && git status -sb          # behind 면 git merge --ff-only origin/main

# 로컬 검증 서버 (스튜디오 테스트에 필수 — /hang 으로 load 를 붙잡는다)
cd design-candidates
python _studio/tools/testserver.py .        # 127.0.0.1:8124

# 회귀 3종
node _studio/tools/test-posts.js                                             # 28항목
chrome --headless --disable-gpu --dump-dom http://127.0.0.1:8124/_studio/selftest.html   # 146항목
chrome --headless --disable-gpu --dump-dom http://127.0.0.1:8124/_studio/inttest.html    # 87항목
```

- 시간 확인은 **PowerShell `Get-Date`** 로 한다.
  Git Bash 의 `TZ=Asia/Seoul date` 는 tzdata 가 없어 UTC 로 떨어진다(9시간 어긋남).
- 헤드리스 Chrome 을 연달아 띄울 때는 `--user-data-dir` 를 매번 다르게 준다(프로필 잠금 충돌).
- 임시 파일은 `/tmp` 가 아니라 세션 스크래치패드에 쓴다(Node 가 `C:\tmp` 로 해석한다).
